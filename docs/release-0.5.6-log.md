# 0.5.6 审核版本发布日志

## 2026-06-15

### 初始化

- 已阅读 `AGENTS.md`、产品增长、组件化重构和回顾工作流计划与日志。
- 已确认当前源码版本为 `0.5.5`。
- 已确认既有审核包采用 `save-to-feishu-版本-google-review-时间戳.zip` 命名。
- 已确认审核包根目录直接包含 `manifest.json` 等扩展运行文件。
- 已确认用户明确要求生成发布包，因此本次允许执行 `npm run build`。

### 当前进度

- [x] 发布范围与打包规则确认
- [x] 全局版本号和版本文案更新
- [x] 生产构建
- [x] 审核包生成与内容校验
- [x] 差异检查与发布结果记录

### 关键决策

- 不修改插件权限或数据边界。
- 不覆盖或回退工作区中尚未提交的功能改动，本次发布包包含这些当前功能。
- 审核包从全新生产构建的 `dist` 内容生成，不打包项目根目录。

### 全局版本号和版本文案更新

- `package.json`、`package-lock.json` 和扩展清单已更新为 `0.5.6`。
- 本地配置默认版本、配置修复默认值已更新为 `0.5.6`。
- 加载旧配置时会把配置版本归一为当前 `CONFIG_VERSION`，避免继续保存旧版本号。
- 设置页版本文案和侧栏更新卡已更新为 `0.5.6`。
- 更新说明改为：
  - 保存时可快速安排下次回顾；
  - 最近保存可一键管理待回顾。
- 当前版本相关测试数据和断言已同步更新。
- 历史发布日志中的旧版本记录保持不变。

### 生产构建

- 已执行 `npm run build`。
- TypeScript 检查通过。
- 主界面、后台 Service Worker 和内容脚本构建成功。
- 内容脚本样式已复制到生产目录。
- 构建命令退出码为 0。

### 审核包生成与内容校验

- 审核包：`release/save-to-feishu-0.5.6-google-review-20260615-124020.zip`
- 文件大小：约 178 KB。
- SHA-256：`ce1848ce9f1e85ef11f4aa276b08808ff1ba43091ae9b17da41ab551d267f0b0`
- 压缩包根目录直接包含 `manifest.json`，没有额外的 `dist/` 目录层级。
- 压缩包内清单版本为 `0.5.6`，Manifest 版本为 3。
- 压缩包共包含 15 个扩展运行文件。
- 未发现 `.env`、TypeScript 源码、source map、测试、文档或嵌套压缩包。

### 差异检查与发布结果

- `git diff --check`：通过。
- `package.json`、`package-lock.json`、`public/manifest.json` 和 `dist/manifest.json` 版本均为 `0.5.6`。
- `unzip -t`：审核包内全部文件完整，无压缩数据错误。
- 旧版本号仅保留在升级来源测试数据和发布历史说明中。
- 本次未修改扩展权限和 host permissions。
- 本次按发布计划执行生产构建和打包，未额外运行 `npm test`。
- 审核包已可用于 Chrome Web Store 提交审核。
